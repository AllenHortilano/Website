Let sections = document.querSelectorAll ('sections);
Let navLinks = document.querSelectorAll('heaer nav a');

window.onscroll = () => {
sections.array.forEach(sec => {
    Let top = windows.scrollY;
});




};